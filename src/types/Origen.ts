export interface Origen {
    object: string  // Tipo de objeto, en este caso siempre es 'Origin'
    id: string     // Identificador único del lugar de origen
    denomination_short: string  // Nombre corto del lugar de origen
    denomination_long: string   // Nombre largo del lugar de origen
  }
  